$(document).ready(function () {
    //slider
    (function () {
        const $mainSlider = $('#sliderHead');
        const options = {
            dots: true,
        };
        $mainSlider.slick(options);
    })();
});